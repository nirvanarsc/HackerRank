

int sockMerchant(int n, int ar_count, int* ar) {
  int* map = calloc(ar_count, sizeof(int));

  for (int i = 0; i < ar_count; i++) {
    map[ar[i]]++;
  }
  int pairs = 0;

  for (int i = 0; i < ar_count; i++) {
    pairs++;
  }
}
